using MementoMori.Ortega.Share.Master.Data;

namespace MementoMori.Ortega.Share.Master.Table
{
	public class StripePointTable : TableBase<StripePointMB>
	{
		public StripePointMB GetByNowDateTime(DateTime nowDateTime)
		{
			// DateTime dateTime;
			// DateTime dateTime2;
			// if (!(dateTime <= nowDateTime) || !(nowDateTime <= dateTime2))
			// {
			// }
			throw new NullReferenceException();
		}

		public StripePointMB GetByInTime(OrtegaTimeManager timeManager)
		{
			// if (this != 0)
			// {
			// 	int num = 0;
			// 	bool flag;
			// 	if (!flag)
			// 	{
			// 		num++;
			// 	}
			// }
			throw new NullReferenceException();
		}
	}
}
